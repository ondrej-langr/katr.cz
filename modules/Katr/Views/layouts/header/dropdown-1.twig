<div x-show="currrentSubmenu=='dropdown-menu-1'"
  x-on:mouseover="currrentSubmenu='dropdown-menu-1'"
  style="display: none;"
  x-transition>
  <div id="dropdown-menu-1" class="submenu__item">
    {% for page in allPages %}
      <div class="col-12 col-lg-3 col-md-4 col-sm-6 col-xl-3 col-xxl-3 dropdown-box">
        <a href="{{ language }}/{{ page.slug }}">{{ page.title }}</a>
        {% if page.excerpt %}
          <p>
            {{ page.excerpt }}
          </p>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>
